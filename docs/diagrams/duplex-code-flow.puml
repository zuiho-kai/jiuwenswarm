@startuml duplex-code-flow
skinparam defaultFontName Microsoft YaHei
skinparam defaultFontSize 14
skinparam shadowing false
skinparam backgroundColor white
skinparam sequenceMessageAlign left
skinparam BoxPadding 10
skinparam ArrowColor #64748B
skinparam NoteBackgroundColor #FFF8DA
hide footbox
legend top left
  |= 颜色 |= 相对原版 Jiuwen 的含义 |
  | <back:#DCEEFF><color:#2563EB> 蓝色 · 新增 </color></back> | 新增类 / 函数 / 命令，以及新增的调用步骤 |
  | <back:#FFE5C2><color:#C56A00> 橙色 · 包装 / 修改 </color></back> | 原入口、回调或分派方法被包装 / 覆盖的接入点 |
  | <back:#EDF1F5><color:#64748B> 灰色 · 原有 </color></back> | 原 SDK 逻辑，直接复用 |
  参与者底色标文件 / 类来源；箭头和【标签】标具体步骤。
  新增子类中调用 super() 的原方法仍标灰色；蓝色不表示被调用的 SDK 方法是新写的。
endlegend
title 双工调用链：正常执行与新请求处理并行\nA = 正常执行循环；B = 新消息到达后才触发。两条流程分别编号。
participant "原 SDK 入口\nMessageHandler\nAgentLifecycleHandler" as entry #EDF1F5
participant "duplex_shadow.py\n入口包装函数\ndeliver_routed()" as adapter #DCEEFF
participant "判断保护：限时、检查结果\nduplex_router.py\nobserve() / validate_decision()" as policy #DCEEFF
participant "发送快模型请求\nduplex_shadow.py\nclassify_input()" as fast #DCEEFF
participant "duplex_native.py\nDuplexNativeHarness\n继承 NativeHarness" as native #DCEEFF
participant "原 SDK\nReActAgent\n慢模型 / 工具" as react #EDF1F5

note over entry, react #FFE5C2
  【包装 / 修改】安装接入点：bootstrap.configure_agent_teams_home() → install_shadow_observer()
  包装 MessageHandler._format_message、TeamAgent.deliver_input、AgentLifecycleHandler.on_user_input；
  active 模式下 Native 工厂创建 DuplexNativeHarness。SDK 文件本身未修改；橙色表示运行时接入或回调被包装。
end note

note over entry, react #E8F5E9
  <b>A 与 B 并行：A 持续执行原任务；新消息可在 A 的模型生成或工具执行期间到达。</b>
  没有新消息时，只执行 A。
  B 判断期间 A 继续运行；APPEND 等待 A 后续接纳，INTERRUPT 暂停 A、作废旧计划后重新规划。
end note

par A · 正常流程：执行原任务，不依赖新消息到达
autonumber 1 "<b>A0</b>"
loop 任务未完成且未暂停：每次慢模型调用
react -[#64748B]> native : <color:#64748B>【原有】</color> SDK before_model_call(ctx)\n标记模型阶段；检查是否需要安全暂停
native -[#64748B]-> react : <color:#64748B>【原有】</color> 继续原模型调用
react -[#64748B]> react : <color:#64748B>【原有】</color> 慢模型返回答案或工具调用
opt 本次模型要求调用工具，且未被暂停
  react -[#64748B]> react : <color:#64748B>【原有】</color> 执行工具，结果进入上下文
end
note over native, react
  任务还需继续 → 重复 A；任务完成 → 结束正常循环。
  B 可在上述执行期间触发；B 发起打断时，按下面的暂停分支处理。
end note
end

else B · 新请求流程：有新消息才执行，和 A 并行
autonumber 1 "<b>B0</b>"
opt 新请求到达：用户补充输入 / A2A 普通消息
== B · 接住新消息：保留原文本与身份 ==
alt A2A 普通消息
  entry -[#64748B]> entry : <color:#64748B>【原有】</color> MessageHandler._process_unread_messages()\n原 DB 读取、展开消息
  entry -[#C56A00]> adapter : <color:#C56A00>【包装】</color> 在原消息文本上附带消息 ID 和正文，供路由识别\n_format_message() 的 render 包装
  adapter -[#2563EB]-> entry : <color:#2563EB>【新增】</color> 返回原投递文本，并附上路由所需的消息信息\nRoutedInput(原 SDK 渲染文本, InboundMessage)
  entry -[#C56A00]> adapter : <color:#C56A00>【包装】</color> 原投递入口先经过路由判断\nTeamAgent.deliver_input() 的 deliver_with_route 包装
else 用户输入
  entry -[#C56A00]> adapter : <color:#C56A00>【包装】</color> 给用户消息附上 ID 和正文，接入同一路由\non_user_input() 的 route_user_input 包装
  adapter -[#64748B]> entry : <color:#64748B>【原有】</color> 原 on_user_input(event)\ncontent 已包装为 RoutedInput
  entry -[#C56A00]> adapter : <color:#C56A00>【包装】</color> 原投递入口先经过路由判断\nTeamAgent.deliver_input() 的 deliver_with_route 包装
end
adapter -[#2563EB]> adapter : <color:#2563EB>【新增】</color> 决定走原投递，还是先问快模型\ndeliver_routed(host, content, use_steer, original)
note over adapter, policy
  RoutedInput 是 str 子类：保留原投递文本，附带 .message。
  InboundMessage = message_id + sender + content（原消息正文）。
  模板 / 审批 / 控制协议不通过 A2A 普通消息包装。
end note

alt 路由未开启 / 非 DuplexNativeHarness / 无可用运行状态 / 原 steer 或 serial 策略
  adapter -[#64748B]> native : <color:#64748B>【原有】</color> original TeamAgent.deliver_input()\n→ TeamHarness.send() → NativeHarness.send()
  native -[#64748B]-> adapter : <color:#64748B>【原有】</color> 原 SDK 投递返回
else 当前实例已经接收过 message_id
  adapter -[#2563EB]> adapter : <color:#2563EB>【新增】</color> 当前实例已成功接收此消息，避免重试时重复投递\n_duplex_received 命中，直接返回
else 当前任务运行中，执行一次快模型判断
  == B · 判断新消息：补充现有任务，还是让当前工作失效？ ==
  adapter -[#2563EB]> native : <color:#2563EB>【新增】</color> 读取慢 Agent 正在做什么，给快模型判断依据\nsnapshot_from_native(native)
  native -[#2563EB]-> adapter : <color:#2563EB>【新增】</color> 返回一份“当前任务信息” ControlSnapshot\n任务目标、当前计划项、执行阶段 + 本地状态标识
  note over adapter, native
    <b>这一步只是读现有信息，不调用模型，也不修改慢模型上下文。</b>
    goal：当前任务目标。优先读已有计划，没有则用原始任务 active.original_query。
    next_action：当前计划项的描述；没有计划项时为空，不让慢模型额外总结。
    phase：目前处在模型生成、工具执行等哪个阶段。
    这三个字段会发给快模型，帮助它判断新消息是否推翻当前工作。
    例：目标“实现订单系统”，当前项“使用 Kafka”；新消息“禁止使用 Kafka” → 倾向 INTERRUPT。

    context_version：本地比较标识，用于判断“等待快模型期间，慢 Agent 状态是否已变化”。
    它由执行轮次、安全点、阶段、任务信息、暂停状态和 SDK 已有发送序号 seq_counter 计算。
    round_id / checkpoint_id：当前执行轮次 / 安全点编号，用于本地比较与记录。
    这三个标识不发给快模型，也不要求快模型回传。
  end note
  adapter -[#2563EB]> policy : <color:#2563EB>【新增】</color> 发起一次有超时限制的判断\nobserve(当前任务信息, 新消息, 判断函数, 重新读取状态的函数, 超时时间)
  note over adapter, policy
    observe() 是普通 Python 函数，不是另一个 Agent。
    它负责：等待一次判断 → 检查结果和当前状态 → 返回是否可用。
    classify 是“怎样调用快模型”的函数；current_snapshot 是“重新读当前状态”的函数。
    timeout_seconds 是等待上限；超时或异常就交回原 steer。
  end note
  policy -[#2563EB]> fast : <color:#2563EB>【新增】</color> 调用传入的判断函数，把任务信息和新消息交给快模型\nclassify(snapshot, messages) → classify_input()
  fast -[#2563EB]> fast : <color:#2563EB>【新增】</color> 准备独立的快模型调用对象\ncreate_tiny_agent(SYSTEM_PROMPT, default_schema=decision_schema(), max_iterations=1)
  note right of fast
    create_tiny_agent() 复用 SDK 的模型调用封装。
    SYSTEM_PROMPT：告诉快模型 APPEND / INTERRUPT 的判定规则。
    decision_schema()：规定回复只能有 action，值只能是这两个之一。
    max_iterations=1：限制这次判断的 Agent 执行轮数。
    这里只准备调用对象；下一步 agent.run() 才提交判断请求。
  end note
  fast -[#2563EB]> fast : <color:#2563EB>【新增】</color> 拼好输入并提交判断请求\nagent.run(prompt_for(snapshot, messages))
  note right of fast
    prompt_for() 把数据写成 JSON 字符串：
    snapshot = {goal, next_action, phase}
    messages = [{message_id, sender, content}]
    例如 content = “禁止使用 Kafka”。
    快模型只判断，不接管原任务，也不获得完整慢模型对话。
    期望回复：{"action":"INTERRUPT"} 或 {"action":"APPEND"}。
    超时时间默认沿用该快模型的 SDK 配置。
  end note
  fast -[#2563EB]-> policy : <color:#2563EB>【新增】</color> 返回建议：增量注入 或 打断\n{action: APPEND / INTERRUPT}
  policy -[#2563EB]> policy : <color:#2563EB>【新增】</color> 检查回复格式，再确认判断依据有没有过期\nvalidate_decision()；current_snapshot() 与请求前的信息比较
  note over policy, native
    为什么要再读一次？快模型判断期间，慢 Agent 仍在执行。
    若慢 Agent 已换阶段、计划或执行轮次，刚才的 INTERRUPT 可能已不适用。
    当前做法：信息有变化就标为 stale（过期），退回 steer，不再重判。
  end note
  policy -[#2563EB]-> adapter : <color:#2563EB>【新增】</color> 返回判断结果及是否可用，供入口选执行路径\nObservation(proposed_action, status, context_version, latency_ms, attempts=1)
  note over adapter, policy
    proposed_action：快模型建议；status：ok / stale / error / timeout。
    context_version：若要打断，交给执行端做最后一次状态检查。
    latency_ms / attempts：耗时与尝试次数记录，不是额外执行步骤。
    只有判断有效且建议 INTERRUPT 才尝试打断；其余走原 steer。
  end note
  note over adapter, react
    B14—B20 合起来只调用一次快模型。
    <b>A 的模型 / 工具继续运行；B 的这条消息等待判断返回。</b>
  end note

  == B · 将判断结果作用到 A：补充或打断 ==
  alt APPEND，或失败 / 超时 / 非法结果 / 判断已过期
    adapter -[#2563EB]> native : <color:#2563EB>【新增】</color> 把新消息交给原增量注入入口\nsteer() 内调用 original TeamAgent.deliver_input()\n→ TeamHarness.send(immediate=True)\n→ NativeHarness.send()
    native -[#64748B]> native : <color:#64748B>【原有】</color> 原 _control 入 _CmdSend；原 _supervisor 取命令
    native -[#64748B]> native : <color:#64748B>【原有】</color> super()._dispatch(_CmdSend)
    native -[#64748B]> native : <color:#64748B>【原有】</color> 原 _on_send() 处理发送
    native -[#64748B]> native : <color:#64748B>【原有】</color> RUNNING：_push_steer(原投递文本)
    native -[#64748B]-> adapter : <color:#64748B>【原有】</color> 原发送 ack
    adapter -[#2563EB]> adapter : <color:#2563EB>【新增】</color> 记录此消息已成功投递，供重试去重\n_duplex_received.add(message_id)
    note over native, react : <b>A 继续当前执行；APPEND 不取消模型，不额外补执行轮次。</b>
    note over native, react
      后续由原 ReActAgent 在下一次模型调用前执行：
      _drain_steering_batch(ctx) → _admit_user_message(source="steering")。
      B 的投递确认不等待这一步完成。
    end note
  else INTERRUPT
    adapter -[#2563EB]> native : <color:#2563EB>【新增】</color> 请求内部打断，并携带原消息和判断时的状态标识\ninterrupt(text, version, message_id)
    native -[#2563EB]> native : <color:#2563EB>【新增】</color> 让 SDK 原命令循环统一处理打断，避免在入口直接改执行状态\n_control 放入 _RouteInput → _supervisor → _dispatch → _route()
    native -[#2563EB]> native : <color:#2563EB>【新增】</color> 再检查 RUNNING、当前版本、重复 ID\n以及是否已有待完成内部打断
    alt 检查不通过：STALE
      native -[#2563EB]-> adapter : <color:#2563EB>【新增】</color> "STALE"
      adapter -[#2563EB]> native : <color:#2563EB>【新增】</color> 原 steer；不执行旧打断
      native -[#64748B]-> adapter : <color:#64748B>【原有】</color> 投递返回
    else 检查通过
      native -[#2563EB]> native : <color:#2563EB>【新增】</color> 记住待完成的内部打断，区分用户取消并在暂停后接续消息\n_duplex_pending = cmd
      native -[#64748B]> native : <color:#64748B>【原有】</color> 交给 SDK 选择如何安全暂停\nsuper()._on_pause(_CmdPause)
      alt 正在生成：active.model_call_in_flight
        native -[#64748B]> react : <color:#64748B>【原有】</color> 取消正在执行的慢模型生成\n原 SDK _hard_cancel_round(active)
        react -[#C56A00]> native : <color:#C56A00>【包装】</color> 检查这次取消是否由内部打断触发\n_handle_context_abort() 包装
        native -[#C56A00]-> react : <color:#C56A00>【包装】</color> 内部暂停不提交用户取消标记
        native -[#64748B]> native : <color:#64748B>【原有】</color> _reset_coordinator() → PAUSED
      else 工具 / 边界阶段
        native -[#64748B]> native : <color:#64748B>【原有】</color> 原 SDK pause_requested=True → PAUSING
        react -[#64748B]> react : <color:#64748B>【原有】</color> 工具执行完成；SDK PhaseSnapshotRail\n在模型边界 cooperative_stop
        react -[#64748B]> native : <color:#64748B>【原有】</color> 原 _CmdRoundFinished
        native -[#C56A00]> native : <color:#C56A00>【包装】</color> _on_round_done() → super()._on_round_done()\nSDK 收尾为 PAUSED
      end
      native -[#2563EB]> native : <color:#2563EB>【新增】</color> _finish_transaction()：清空 task_plan 并保存状态\n保留有效需求与工具结果；旧计划不再作为待执行任务
      native -[#2563EB]> native : <color:#2563EB>【新增】</color> paused_query 替换为重新规划指令 + 新消息\n明确旧计划作废，队友消息不能覆盖用户要求
      native -[#64748B]> native : <color:#64748B>【原有】</color> super()._on_send(_CmdSend(replan, immediate=True))\nPAUSED 分支：_start_round(replan, resume_continuation=True)\n_push_steer(重新规划指令 + 新消息)
      native -[#64748B]> react : <color:#64748B>【原有】</color> 根据新消息重新规划，再回到 A
      native -[#2563EB]-> adapter : <color:#2563EB>【新增】</color> 通知投递入口：打断并接续已完成\nack="INTERRUPT"；记录 message_id
    end
  end
end

== B · 投递返回，原入口确认 ==
adapter -[#2563EB]-> entry : <color:#2563EB>【新增】</color> 投递完成后返回
opt A2A 消息
  entry -[#64748B]> entry : <color:#64748B>【原有】</color> 投递成功后，由原入口把 A2A 消息标为已读\nMessageHandler → TeamMessageManager.mark_messages_read()
end
note over entry, native
  已读确认后不承诺进程崩溃恢复；没有独立收件库或工具账本。
  用户 pause / abort / resume / stop 取代尚未完成的内部打断：
  _reject_transaction() 使其失败，不自动恢复；A2A 投递失败的消息保留未读。
end note
end
end
@enduml
