@startuml duplex-routing-flow
title 一条新消息的处理流程
skinparam defaultFontName Microsoft YaHei
skinparam defaultFontSize 17
skinparam shadowing false
skinparam backgroundColor #FFFFFF
skinparam ArrowColor #52647A
skinparam activityBackgroundColor #E1F0FF
skinparam activityBorderColor #6A8BAB
skinparam activityDiamondBackgroundColor #FFF1CC
skinparam noteBackgroundColor #F5F6F8
skinparam noteBorderColor #B8C1CC

start
:① 慢 Agent 正在干活\n此时收到一条新消息;
:② 接住消息，交给快模型判断;
note right
  慢 Agent 此时继续工作
  又来消息就合起来重新判断
end note
if (③ 新消息需要马上改变当前工作吗？) then (不需要)
  :④A 当前步骤继续\n下一次模型调用前加入新消息;
else (需要)
  :④B 安全停下当前工作\n加入新消息，保留已完成的结果;
  note right
    谁来停？慢 Agent 自己的执行器。
    正在生成：取消当前生成，从安全点继续。
    工具已经开始：先等工具完成，保留结果。
  end note
endif
:⑤ 同一个慢 Agent\n按新消息继续任务;
stop

legend bottom
  **快模型只判断；慢 Agent 的执行器负责真正停下和继续。**
  判断超时或失败，不应用决定；DB 保持未读。
  图中只展开一次消息处理；慢 Agent 继续后仍可接收新消息。
  去重、版本检查、已读确认等保护细节由代码处理，不展开成主流程。
endlegend
@enduml
