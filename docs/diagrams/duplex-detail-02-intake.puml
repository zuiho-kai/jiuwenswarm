@startuml duplex-detail-02-intake
title ② 接住消息：展开总览第 ② 步
skinparam defaultFontName Microsoft YaHei
skinparam defaultFontSize 16
skinparam shadowing false
skinparam activityBackgroundColor #E1F0FF
skinparam activityDiamondBackgroundColor #FFF1CC
skinparam noteBackgroundColor #F5F6F8
start
:②.1 原 SDK 接收消息
DB 事件 / 轮询，或原用户入口;
:②.2 原处理器按顺序投递
控制、审批、模板消息仍走原逻辑;
if (当前实例已经接收过这个 ID？) then (是)
  :不重复投递
DB 输入重试原已读确认;
  stop
else (否)
endif
:②.3 交给判断层
相同 ID 复用处理结果，内容冲突报错;
:②.4 进入第 ③ 步判断;
stop
legend bottom
  不新增 DB 轮询、事件派发任务或接收容量限制。
  DB 按原 SDK 顺序送入；只有真正并发到达判断层的输入才合批。
  本步骤不确认已读；第 ④ 步先持久保存输入，再确认接收。
  对应代码：原 MessageHandler / duplex_shadow.py / duplex_controller.py
endlegend
@enduml
