@startuml duplex-benchmark-flow
title 官方 Benchmark：执行与评分流程
skinparam defaultFontName Microsoft YaHei
skinparam defaultFontSize 14
skinparam shadowing false
skinparam ArrowColor #52647A
skinparam activityBackgroundColor #E1F0FF
skinparam activityBorderColor #6A8BAB
skinparam activityDiamondBackgroundColor #FFF6DC
skinparam noteBackgroundColor #FFF6DC
skinparam backgroundColor #FFFFFF

start
:duplex_experiment\n加载外部配置，创建全新结果目录;
:校验上游固定 commit 与 tracked 文件\n选择官方原题，保留原 updates / rubric;
if (评测集？) then (InterruptBench / U2A)
  partition "原 WebArena runner：Python 3.11" {
    :外部提供网站 / reset 服务\n强制每题 reset 配置，生成原始意图配置;
    repeat
      :运行官方 baseline，保存轨迹;
      repeat
        :默认策略 official / steer / abort_restart / model;
        repeat
          :上游 make_multi_interrupt_stage\n由上一阶段轨迹生成当前意图与更新边界;
          note right
            Stage 1 共享本轮 baseline。
            后续阶段使用各策略自己的轨迹。
          end note
          :原 runner reset → replay\n到达官方 K-action 更新边界;
          if (official 策略？) then (是)
            :沿用原 call_llm 与原更新时序;
          else (并发扩展)
            :原 PromptAgent 构造更新前 / 后 prompts;
            :ProcessWorker：stdio JSON\n独立 Python 3.12 执行 NativePeer;
            :启动旧 prompt 的慢模型请求\n异步投递官方原始 update;
            note right
              更新仍在同一 K-action 边界。
              等待期间不执行新网页动作。
              独立 worker 的日志走 stderr。
            end note
            :应用 Native 策略，返回文本动作;
          endif
          :原动作解析 → env.step → 保存轨迹\n原 evaluator_router 评分;
        repeat while (还有更新阶段？) is (是) not (否)
      repeat while (还有策略？) is (是) not (否)
    repeat while (还有重复实验？) is (是) not (否)
  }
else (AgentRadio / A2A)
  partition "原 Harbor + Linux 任务容器" {
    :准备 Docker 或已认证 Modal\n原任务容器与 Coral 通信服务;
    repeat
      repeat
        :默认策略 L2 / L3 / steer / abort_restart / model;
        if (L2 / L3？) then (是)
          :上游 Coral 适配器与原 peer 执行器;
        else (Native 策略)
          :JiuwenAgentRadio 继承原安装与编排\n仅替换 peer 启动入口;
          :原四 Agent、五阶段提示与恢复提示\n启动持久 NativePeer;
          :Bash watcher 后台完成\n原 Coral 消息输出 → NativePeer.receive;
          :按策略追加 / 取消重建 / 安全恢复\n继续任务直到答案产出或失败;
        endif
        :保留答案和 trial 产物\n原 verify_local.py 按 rubric 评分;
      repeat while (还有策略？) is (是) not (否)
    repeat while (还有重复实验？) is (是) not (否)
  }
endif
:duplex_metrics 汇总原评分与运行事件\n末阶段按任务配对，task bootstrap 置信区间;
:写 report.json、execution.json 和逐步日志\n缺失分数留在分母，缺失成本指标为 null;
stop

legend bottom
此图描述已接入的代码流程，不表示已完成公开任务实测或获得性能收益。
任一步骤失败会保留日志并终止；不跳过失败题以提高成绩。
AgentRadio 仍走 Coral；Jiuwen DB 消息链路由另行集成测试覆盖。
endlegend
@enduml
