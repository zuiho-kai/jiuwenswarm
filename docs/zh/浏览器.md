# 浏览器工具说明

> **说明：** 本页介绍的是**服务器端托管浏览器**（智能体的 Chrome 驱动）。若想了解**浏览器扩展**
> （将 JiuwenSwarm 带到你阅读的任意页面旁），请参见 [浏览器扩展](browser-extension/浏览器扩展.md)。

## 1. 概述

JiuwenSwarm 浏览器工具用于驱动真实 Chrome 完成网页访问、表单填写、页面点击、
文件上传和信息提取等任务。

Chrome 由浏览器 Agent 统一管理。用户只需在前端配置 Chrome 可执行文件和显示
模式，Agent 会在首次执行浏览器任务时自动启动浏览器，不再需要从前端单独启动
浏览器服务。

JiuwenSwarm 内置不含浏览器二进制的 `@playwright/mcp@0.0.78` 运行时，正常路径不会访问 npm。
Chrome 本身不随产品分发。桌面版与 Docker 镜像内置 Node 22.11.0；pip/wheel 和源码安装仅在
启用浏览器运行时功能时需要 Node.js 20 或更高版本。普通启动和非浏览器 Agent 不依赖 Node.js。

浏览器工具支持：

- 打开网页并等待页面加载
- 点击元素、输入文本和上传文件
- 执行多步骤网页任务
- 复用会话及其登录状态
- 读取页面标题、URL 和页面内容

## 2. 快速上手

### 2.1 安装 Chrome

请先在运行 JiuwenSwarm 的机器上安装 Google Chrome。托管驱动通常可以自动识别
标准安装位置。

如果 Chrome 安装在自定义目录，可打开 `chrome://version`，复制"可执行文件路径"，
并在下一步中配置。

### 2.2 配置浏览器

1. 打开 JiuwenSwarm 前端。
2. 进入"设置" > "浏览器"。
3. 按需填写 Chrome 可执行文件的完整路径；留空时使用自动检测。
4. 需要观察浏览器或人工登录时，启用"显示浏览器"；无需人工操作时可关闭。
5. 保存设置。

显示模式发生变化时，系统会在必要时重启浏览器运行时，确保后续任务使用新的模式。

### 2.3 执行浏览器任务

直接让 Agent 打开网页、提取信息、填写表单或继续已授权流程。浏览器 Agent 会自动
启动并管理 Chrome。

在显示模式下，首次浏览器任务开始时会弹出 Chrome 窗口。如任务需要登录、MFA、
扫码或其他人工授权，请在该托管窗口中完成，然后回到对话继续任务。

### 2.4 常见问题排查

如果浏览器任务执行失败，可以按照以下步骤排查：

| 问题现象 | 可能原因 | 解决方法 |
|----------|----------|----------|
| 首次浏览器任务无响应 | Chrome 路径配置错误 | 检查 `config.yaml` 中的 `browser.chrome_path` 是否正确，确保路径指向真实存在的 `chrome.exe` |
| Chrome 启动后立即关闭 | 端口被占用 | 非 keyed 实例：检查 `9333` 端口是否被其他进程占用并释放；keyed 实例：确认自动分配机制正常工作，若仍失败则检查系统防火墙或端口范围限制 |
| Chrome 未安装 | Chrome 未安装或路径错误 | 安装 Chrome 浏览器，并更新 `browser.chrome_path` 配置 |
| 前端提示连接失败 | 浏览器运行时未正常启动 | 检查后端服务日志，确认浏览器 Agent 是否成功启动 |
| 无法打开特定网页 | 网络或权限问题 | 检查网络连接，确保目标网站可访问，必要时在托管 Chrome 窗口中手动测试 |

## 3. 使用建议

- 依赖登录态的长流程尽量保持在同一个 Agent 会话中。
- 登录、MFA、扫码和授权场景使用显示模式。
- 不需要人工交互的任务可使用无头模式。
- 正常使用 JiuwenSwarm 时，不要再额外启动使用固定调试端口的 Chrome；托管驱动会
  自动选择并注入正确的 CDP 地址。
- 除非明确需要短超时，建议保持 `BROWSER_ALLOW_SHORT_TIMEOUT_OVERRIDE=0`。

## 4. 案例

### 4.1 提取网页信息

1. 在对话中输入："请从 https://example.com/news 提取今天的新闻标题和摘要。"
2. Agent 会自动启动托管浏览器、访问页面并返回结果。

### 4.2 发送带附件的邮件

1. 启用"显示浏览器"并保存。
2. 让 Agent 打开 Gmail。
3. 如有需要，在托管 Chrome 窗口中完成登录。
4. 让 Agent 编写邮件并上传附件。

## 5. 配置

### 5.1 `config.yaml`

| 配置项 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `browser.chrome_path` | string/map | `""` | Chrome 可执行文件。留空时自动检测，也可按操作系统配置路径。 |
| `browser.headless` | boolean | `true` | 设为 `false` 时显示托管浏览器窗口。 |

示例：

```yaml
browser:
  chrome_path:
    windows: "C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"
    macos: "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
    linux: "/usr/bin/google-chrome"
  headless: true
```

`chrome_path` 留空时会清除之前配置的可执行文件，由 openJiuwen 从系统中自动检测
Chrome。填写路径后，该路径是唯一依据；如果它不是有效的 Chrome 可执行文件，浏览器
启动会直接报错，不会静默改用其他安装路径。

### 5.2 高级环境变量

大多数安装无需设置以下变量。`BROWSER_MANAGED_*` 系列是运行时内部变量，由系统根据 `config.yaml` 的 `browser.*` 配置自动派生设置，**不需要在 `.env` 中手动配置**；这里列出以便高级用户排查或覆盖。

| 环境变量 | 默认值 | 说明 |
|---|---|---|
| `BROWSER_DRIVER` | JiuwenSwarm 中为 `managed` | 浏览器驱动模式。 |
| `BROWSER_PROFILE_NAME` | `Default`（.env.template）/ `jiuwenclaw`（代码运行时默认） | 浏览器 Profile 名称；见 `.env.template` 第 101 行。 |
| `BROWSER_MANAGED_BINARY` | 自动检测（来自 `browser.chrome_path`） | 托管 Chrome 可执行文件路径；运行时由系统根据 `config.yaml` 的 `browser.chrome_path` 自动设置。 |
| `BROWSER_MANAGED_PORT` | `9333` | 非 keyed 托管 Chrome 实例的端口（与 `BROWSER_RUNTIME_MCP_PORT` 不同；后者是 MCP wrapper 端口，默认 8940）；keyed 实例自动分配空闲端口。 |
| `BROWSER_MANAGED_USER_DATA_DIR` | 托管 Profile 目录（默认在运行时状态根下） | 覆盖托管浏览器的 Profile 目录。 |
| `BROWSER_MANAGED_ARGS` | 根据显示模式生成（如 `--headless=new`） | 额外的 Chrome 启动参数；运行时根据 `browser.headless` 等派生。 |
| `BROWSER_MANAGED_KILL_EXISTING` | `false` | 启动前终止匹配的既有 Chrome。仅在明确 Profile 所有权时启用。 |

`PLAYWRIGHT_CDP_URL` 用于显式的远程驱动模式，正常的托管浏览器流程不需要配置它。

#### 5.2.1 浏览器 MCP 运行时变量

以下变量控制浏览器 MCP 运行时 wrapper 的启动与通信方式（见 `.env.template` 第 64-86 行；MCP 配置详见 [MCP 配置](MCP配置.md)）。

| 环境变量 | 默认值 | 说明 |
|---|---|---|
| `BROWSER_RUNTIME_MCP_ENABLED` | `1` | 浏览器 MCP 运行时总开关（0 关闭 / 1 启用）。 |
| `BROWSER_RUNTIME_MCP_CLIENT_TYPE` | `streamable-http` | MCP 客户端类型：`stdio` / `sse` / `streamable-http`（推荐）。 |
| `BROWSER_RUNTIME_MCP_SERVER_ID` | `playwright_runtime_wrapper` | MCP server ID（注册标识）。 |
| `BROWSER_RUNTIME_MCP_SERVER_NAME` | `playwright-runtime-wrapper` | MCP server 显示名。 |
| `BROWSER_RUNTIME_MCP_SERVER_PATH` | `http://127.0.0.1:8940/mcp` | MCP server 端点 URL（`streamable-http` 模式默认 `/mcp`，`sse` 模式默认 `/sse`，`stdio` 模式下仅为元数据）。 |
| `BROWSER_RUNTIME_MCP_TIMEOUT_S` | `300` | MCP 请求超时（秒）。 |
| `BROWSER_RUNTIME_MCP_HOST` | `127.0.0.1` | 本地 wrapper 主机（用于自动启动）。 |
| `BROWSER_RUNTIME_MCP_PORT` | `8940` | 本地 wrapper 端口（与 `BROWSER_MANAGED_PORT` 不同；后者是托管 Chrome 实例端口，默认 9333）。 |
| `BROWSER_RUNTIME_MCP_PATH` | `/mcp` | 本地 wrapper 路径（用于自动启动）。 |
| `BROWSER_RUNTIME_MCP_COMMAND` | 空 | `stdio` 模式启动命令覆盖（空表示用当前 Python 可执行文件）。 |
| `BROWSER_RUNTIME_MCP_ARGS` | 空 | `stdio` 模式启动参数覆盖（空表示用内置默认值）。 |
| `BROWSER_RUNTIME_MCP_AUTO_SSE_FALLBACK` | `1` | `stdio` 模式优先尝试 SSE 回退（1 启用，推荐）。 |

## 6. 架构

浏览器生命周期为：

`前端保存设置 -> Agent 浏览器任务 -> 自动启动托管 Chrome -> 执行任务 -> 复用会话`

- 前端 `BrowserPanel` 只负责读取和保存 Chrome 路径与显示模式。
- JiuwenSwarm 将这些设置映射到浏览器 Agent 运行时。
- openJiuwen 的 `BrowserService` 和 `ManagedBrowserDriver` 负责按需分配端口、启动
  Chrome、健康检查、复用 Profile，并随 Agent 生命周期停止。
- Playwright MCP 使用托管实例注入的端点，不再依赖前端启动的浏览器。

### 6.1 核心代码

浏览器工具的核心代码主要分布在以下模块：

| 模块 | 文件路径 | 功能说明 |
|------|----------|----------|
| 前端浏览器服务面板 | `jiuwenswarm/channels/web/frontend/src/components/BrowserPanel/index.tsx` | 负责读取路径、保存路径 |
| 后端 Web RPC 入口 | `jiuwenswarm/gateway/channel_manager/web/app_web_handlers.py` | 提供 `path.get`、`path.set` 等前端调用入口 |
| 浏览器 Subagent 接入 | `openjiuwen.harness.subagents.browser_agent` | 浏览器 Subagent 配置与生命周期接入 |
| Chrome 启动与管理 | `openjiuwen.harness.tools.browser_move.drivers.managed_browser` | `ManagedBrowserDriver` 实现：端口分配、Chrome 进程管理、Profile 复用 |
| 浏览器运行时编排层 | `openjiuwen.harness.tools.browser_move.playwright_runtime.runtime` | 浏览器 runtime 编排层 |
| 浏览器任务执行 | `openjiuwen.harness.tools.browser_move.playwright_runtime.service` | 浏览器任务执行、session 复用、timeout guardrails、驱动生命周期管理 |
| 浏览器运行时配置 | `openjiuwen.harness.tools.browser_move.playwright_runtime.config` | Playwright MCP 与浏览器运行时配置解析 |

## 7. 一句话总结

浏览器工具的本质，是让 Agent 在用户已经授权过的真实 Chrome 上执行网页操作；前端负责配置，后端负责自动启动和自动化执行。
---

## 返回导航

- [返回文档首页](../README.md)
- [返回项目首页](../../README_CN.md)
