# SpreadsheetBench Verified 13-1：看护修复后整题重跑

**两组均 120/120 正确；普通组正常完成，全双工组超时。两组均无纠错消息、无打断，未证明全双工提速，也不能把此次得分改善归因于看护修复。**

| A2A | 总耗时 | 单元格正确 | 原题通过 | 实际打断 | 停止原因 |
|---|---:|---:|---:|---:|---|
| 普通（steer） | 272.2 秒 | 120/120 | 1/1 | 0 | 正常完成 |
| 全双工（model） | 601.3 秒 | 120/120 | 1/1 | 0 | 600 秒上限超时 |

全双工组多用 **329.1 秒**，运行时长约为普通组 **2.21 倍**。两组首次保存表格分别在 138.6 / 274.4 秒；全双工在 591.1 秒完成最后一次工具检查，随后生成最终回复时到达上限，已保存的表格通过原始评分。

## 时间花在哪里

| 指标 | 普通 | 全双工 |
|---|---:|---:|
| 执行模型请求数 | 9 | 11（末次超时取消） |
| 执行模型请求累计耗时，含服务等待 | 264.1 秒 | 595.4 秒 |
| 执行模型输入 / 输出 token（已记录） | 70,804 / 5,727 | 106,913 / 8,437 |
| Bash 累计耗时 | 5.2 秒 | 4.5 秒 |
| 看护调用 | 1 | 3 |
| 发出的纠错消息 / 路由调用 | 0 / 0 | 0 / 0 |

全双工执行 Agent 生成了更多脚本和检查，并多次继续自检；其中一次模型调用用了 116.0 秒。时间差主要体现在执行模型请求内，不能将整段差额当作双工框架开销。两组独立生成，即使模型和初始配置一致，也不是同一条执行轨迹。

Token 为服务已返回的用量，未包含取消请求可能尚未返回的用量。

## 看护仍有问题

- 普通组：唯一一次复核在 13.9 秒后因未提交有效结构化结果失败，没有给出意见。
- 全双工组：一次复核超过 45 秒；一次“未清理旧数据”意见在 16.2 秒后因证据未逐字匹配被拒绝；最后一次用 5.1 秒确认排序正确。
- “未清理旧数据”属于误报：实际 `process_workbook.py` 第 104–108 行清空了非示例区的旧单元格。确认模型也误判支持，只是最终原文校验拦住了消息。
- 本次看护没有发出有效纠错。日志重放通过不等于真实整题运行已经稳定；超时和结构化输出失败仍存在。

## 同题同配置

2026-09-17 在 `wzr@47.79.124.13:31442` 原生环境运行，先普通后全双工，每策略一次。使用冻结源码和相同原始输入、提示、角色、模型配置，运行中未改代码。

- 数据：[SpreadsheetBench Verified 400](https://huggingface.co/datasets/KAKA22/SpreadsheetBench/blob/main/spreadsheetbench_verified_400.tar.gz)，原题 `13-1`，输入 `1_13-1_init.xlsx`；无人工错误或打断注入，未暴露隐藏答案。
- 评分：上游 `cell_level_compare`，范围 `LISTS!A3:D32`，LibreOffice 7.6.7.2。单元格正确数是诊断信息，原题按全部通过计分。
- 执行 / 纠错确认：`DeepSeek-V3.2`；发现 / 路由：`Qwen3.5-9B`。temperature=0、thinking 关闭；输出上限依次为 2048 / 256 / 512 / 256。
- 每组 600 秒、18 次执行模型调用、16 次 Bash 上限；看护和确认的额外调用受看护次数及时间限制，不计入这 18 次。看护上限 45 秒，路由上限 5 秒。
- 耗时包含运行结束清理，不含准备和最终评分。本次只重跑该办公题，非全量榜单成绩。

输入 SHA256：`36e7e5238ac29a506d06a27b73aab4ac6249530c9cfd4b1c40835c7823a39da5`；冻结源码包 SHA256：`be85a1aa8cdcbdb59ca79cad9bc4a544a2a6b8aa2d0b71023c149f88cc74bc61`。摘要脚本已核对两组任务、角色、指令、模型、预算和输入完全一致。

运行目录：`/tmp/wzr-duplex-review-20260916/rerun-13-1-monitor-fixed-20260917-v1`。完整归档：`/workspace/wzr/duplex-benchmark-20260908/office-code-20260915/duplex-13-1-monitor-fixed-rerun-20260917.tar.gz`，旁附 SHA256、摘要与本报告。本地副本位于 `D:\jiusi_agent\_deps\duplex-review-20260916`，包含两组日志、产物、源码、配置清单和原始评分。

前文：[看护修复](duplex-13-1-monitor-fix-20260917.md)、[此前 600 秒复测](duplex-13-1-rerun-20260917.md)。旧结果保持不变。
