# SpreadsheetBench Verified 13-1：看护误拦修复

后续：[整题重跑结果](duplex-13-1-monitor-fixed-rerun-20260917.md)：两组均 120/120；普通 272.2 秒完成，全双工 601.3 秒超时，均无打断。以下保留当时的修复验证记录。

**误拦已修，远程真实日志重放通过：表头偏移能发出纠错，临时查表脚本不再因“功能没写完”被误报。** 本轮验证复核机制，未重跑整道题，不能据此宣称 13-1 已满分或全双工已提速。

改动：从发现中提取能在原日志逐字匹配的片段，再核实错误；原文、解释、修正分开保存，只发送核实后的修正。无法确认记为 `unverified`，不再记成 `ok`。探索阶段只检查已经发生的错误，不要求临时检查命令具备最终产物的完整功能。

Qwen3.5-9B 单独复核仍有误判，因此 **Qwen 负责发现，DeepSeek-V3.2 确认纠错**；执行 Agent 和路由模型未换，普通 / 全双工共用此配置。强制调用 SDK 的结构化输出工具，避免先说一段话、再补问一轮。复核异步执行，上限从 15 秒调为 45 秒；文件版本过期仍丢弃。

| 原始 13-1 日志 | 验证结果 | 时间 | 记录版本 |
|---|---|---:|---|
| 被旧规则拦下的表头偏移发现 | 确认错误，保留表头修正，丢弃额外删除要求 | 9.3 秒 | v5 |
| 误称 LISTS 必须增加 BATCH 列 | 不支持，不发送 | 7.3 秒 | v5 |
| 误称应有 10 个分组 | 不支持，不发送 | 29.2 秒 | v5 |
| 要求临时查表命令实现完整功能 | 不支持，不发送 | 23.3 秒 | v7，最终代码 |
| 从原始命令/输出重新发现表头偏移，经 LiveReviewer 投递出口 | 发现并核实，输出 1 条纠错消息 | 27.6 秒 | v7，最终代码 |

前四项重放原看护的真实发现，末项不提供旧发现、重新调用模型检查。消息投递出口记录在日志中；普通 / 全双工的数据库接线另由集成测试验证。没有注入新故障，也没有把重放当成新数据集成绩。

最终回归 **26 通过、1 跳过**（另 38 项未选）；包含误拦复现、虚构证据拒绝、只发送核实后的修正、单次结构化模型请求及两种 A2A 接线。调试 v1–v3 保留了 Qwen 误判，v4 保留超时，v5 有 1 次 30 秒超时，v6 仍误报探索命令；未从记录中删除。

源码：`app-review-grounding-20260917`。远程归档：`/workspace/wzr/duplex-benchmark-20260908/office-code-20260915/duplex-13-1-monitor-fix-20260917.tar.gz`，附 SHA256；本地副本：`D:\jiusi_agent\_deps\duplex-review-20260916`。包含各版原始结果、日志、源码、最终测试与重放脚本。

之前的整题结果仍见 [13-1 复测报告](duplex-13-1-rerun-20260917.md)，保持原始记录。
