# SpreadsheetBench Verified 13-1：DS v4.1 重跑

**两组均正常提交，原题 120/120 正确。普通 92.0 秒，全双工 62.1 秒；均无消息和实际打断。本轮快了 29.9 秒，但不能归因于打断，也不能用一次运行证明稳定。**

| 指标 | 普通 A2A | 全双工 A2A |
|---|---:|---:|
| 总耗时 | 92.0 秒 | 62.1 秒 |
| 原题通过 / 单元格正确 | 1/1；120/120 | 1/1；120/120 |
| 首次保存表格 | 42.4 秒 | 26.2 秒 |
| 执行模型调用 | 13 | 9 |
| 执行模型累计用时 | 78.7 秒 | 49.7 秒 |
| 执行输出 token | 7,590 | 4,426 |
| 工具调用 / 累计用时 | 14 / 10.4 秒 | 10 / 10.3 秒 |
| 看护调用 | 2 | 1 |
| 纠错消息 / 实际打断 | 0 / 0 | 0 / 0 |
| 最终验证提交 | 1 次成功 | 1 次成功 |

## 解释

- 时间差主要来自执行模型生成的工作量。普通组额外运行了修改输入后的新增数据测试；这次确实向测试副本追加数据，并断言汇总变化，不是复制原输入重复检查。
- 两组都调用 `VerifyAndFinish`，成功后没有追加执行模型调用。
- 普通组看护一次结果过时、一次结构化字段缺失。全双工看护错误声称 REAS 应排在 RETER 后，DS v4.1 确认模型正确驳回，未发送纠错。不能把“0 打断”理解为看护没有运行。

## 同题同条件

服务器 `wzr@47.79.124.13:31442`，原生环境，先普通后全双工，各一次。原始公开数据集 **SpreadsheetBench Verified (400)**，原题 **13-1**；从 `1_13-1_init.xlsx` 独立开始，无预置解答、错误或打断注入。上游 `cell_level_compare` 评分 `LISTS!A3:D32`，LibreOffice **7.6.7.2**。

两组均使用执行/确认模型 `deepseek-v4.1-flash`，看护/路由 `Qwen/Qwen3.5-9B`；temperature=0。每组 600 秒、18 次执行模型调用、16 次工具调用；执行输出上限 **8192**，看护 512、确认/路由 256。看护超时 45 秒，路由 5 秒。时间不含环境恢复、进程导入和最终评分，包含 Agent 启动与清理。

已核对两组任务、提示、角色、配置、输入和首次模型语义请求一致。输入 SHA256：`36e7e5238ac29a506d06a27b73aab4ac6249530c9cfd4b1c40835c7823a39da5`；初始语义请求 SHA256：`4be20d7d2941154d751c37f1e684cfafef28297ca8df0745923d65f931f70b53`。

相比[上一轮 DS v3.2 整题](duplex-13-1-monitor-fixed-rerun-20260917.md)，模型、执行输出上限及最终提交机制都有变化，不能把全部差异只算作换模型的收益。

## 失败记录与参数

- `v1` 沿用 2048 上限，两组分别 48.5 / 20.3 秒结束，但都没有表格，原题 0/1。最后一次请求的 2048 token 全用于思考，工具参数也曾被截断；空回复被 SDK 当成结束。`completed` 不等于任务成功。
- 该接口未可靠遵循 `enable_thinking=false`；尝试 `thinking.type=disabled`、`reasoning_effort=none` 等仍观察到思考 token。因此本轮提高执行输出上限至 8192。最终 `v3` 日志中的 reasoning_tokens 均为 0，不能据此认定关闭参数已稳定生效。
- `v2` 恢复环境时，LibreOffice 可供评分器调用，却未出现在执行工具 PATH；普通组开始搜索程序后终止该轮，补齐命令入口，再从原始输入重新跑两组。`v2` 不纳入对比；执行工具的实际环境已通过转换预检。
- 旧 `/tmp` 已清理，本次从持久化归档恢复 SDK、数据和代码；运行、依赖和证据均放在持久化目录。失败日志全部保留。

## 复现与归档

最终运行：`/workspace/wzr/duplex-benchmark-20260908/office-code-20260915/rerun-13-1-dsv41-20260918-v3`。配置快照在同级项目 `private/models-dsv41-rerun-20260918-v3.json`，权限 0600，不进入归档。

完整归档：`/workspace/wzr/duplex-benchmark-20260908/office-code-20260915/duplex-13-1-dsv41-rerun-20260918.tar.gz`，附 SHA256、摘要和报告。包含最终两组产物、评分、事件、源码/SDK、两轮失败证据及参数探测。本地副本：`D:\jiusi_agent\_deps\duplex-review-20260916`。
